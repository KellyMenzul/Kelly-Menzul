import styled from "styled-components";
import React from "react";

const Holder = styled.di

// margin-top:-160px;
const RegularButton = ({
}) => {
    return <div></div>
}

export default RegularButton;